import { SqaureProps } from '../interfaces';

export const CreateSquare = (children: JSX.Element) => {
  return children;
};

//contextual typing. This helps cut down on the amount of effort to keep your program typed.
// export const addScore: (number: number) => number = function (number) {
//   return (number += 1)
// }

// export const addScoreRobot: (number: number) => number = function (number) {
//   return (number += 3)
// }
